//let evenNumbers = numbers.filter(function(number) {
  //  return number % 2 === 0;
  //});
for (let index = 0; index < 1000; index++){
    if (index < 20) {
      console.log(numbers);
    } else {
      break;
    }
    
  }