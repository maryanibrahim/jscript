let height = 10;
let base = 4;

console.log(`Perimeter of rectangle is two multiply (heights +base ) and the result is ${2*(height+base)}`);
console.log(`Area rectangle is height multiply the base equals to ${height*base}`);
console.log (`height equals to ${height}`);
console.log (`base  equals to ${base}`);